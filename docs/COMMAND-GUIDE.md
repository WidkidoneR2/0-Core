# Faelight Forest -- Core Command Reference

**Version:** 2.0.0  
**Generated:** 2026-05-11  
**Domains:** 67  
**Source:** Auto-generated by faelight-docs

---

## core deploy

Deploy intelligence -- check, record, log

| Command | Description |
|---------|-------------|
| `core deploy check` | Pre-deploy health gate + dependency check |
| `core deploy record` | Record deploy outcome to state.db |
| `core deploy log` | Show recent deploy history |
| `core deploy rollback` | Rollback tool to previous version |
| `core deploy check-deps` | Show full dependency graph for a tool |

---

## core friday

Friday -- The Living Intelligence

| Command | Description |
|---------|-------------|
| `core friday status` | Show what Friday has observed and learned |
| `core friday ask` | Ask Friday a question about the forest |
| `core friday observe` | Trigger observation cycle manually |
| `core friday extract-patterns` | Extract patterns from shell history |
| `core friday suggest` | Get Friday's evidence-based suggestion |
| `core friday update-personality` | Update personality from interaction data |
| `core friday seed-knowledge` | Seed Linux/Rust/Forest knowledge base |
| `core friday learning-loop` | Run the learning loop |
| `core friday name-abstraction` | Name a friday abstraction |
| `core friday vocabulary` | List Friday vocabulary |
| `core friday propose-intent` | Friday proposes a new intent |
| `core friday phase2-init` | Initialize Friday Phase 2 -- temporal models and persistent state |
| `core friday phase2-status` | Show Friday Phase 2 status |
| `core friday plan` | Friday proposes a multi-step strategic plan |
| `core friday temporal-models` | Show Friday temporal models |
| `core friday detect-temporal-patterns` | Detect cross-session temporal patterns and validate models |
| `core friday resolve-contradictions` | Propose resolutions for active contradictions |
| `core friday health-forecast` | Predict health trajectory 24-72h ahead |
| `core friday interrupt-level` | Show trust-modulated interrupt levels for all models |
| `core friday cross-intent-patterns` | Detect patterns spanning multiple intents |
| `core friday phase2-status-full` | Show full Phase 2 status |
| `core friday session-start` | INT-234 -- session lifecycle: start session (fsh launch hook) |
| `core friday session-end` | INT-234 -- session lifecycle: end session (fsh exit hook) |
| `core friday context` | INT-234 -- show current session buffer (last 10 exchanges) |
| `core friday infer` | INT-234 Gate 6 -- run forward-chaining inference across templates |
| `core friday reason` | INT-234 Gate 7 -- chain facts to answer a question |
| `core friday anticipate` | INT-234 Gate 8 -- predict next action using session + temporal models |
| `core friday review` | INT-234 Gate 9 -- session-aware briefing combining inference + anticipation |
| `core friday why` | INT-244 v22 -- query the decision record ("why did we do X?") |
| `core friday decide` | INT-244 v22 -- record an architectural decision |
| `core friday decisions` | INT-244 v22 -- list all recorded decisions |
| `core friday map-show` | INT-244 v22 -- show the system map (tools, deps, health) |
| `core friday map-update` | INT-244 v22 -- update the system map from current state |
| `core friday self-review` | INT-244 v22 -- show Friday self-review for last session |
| `core friday docs` | INT-244 v22 Pillar 1 -- show pending doc proposals |
| `core friday docs-analyze` | INT-244 v22 Pillar 1 -- analyze last commit for doc proposals |
| `core friday docs-approve` | INT-244 v22 Pillar 1 -- approve a doc proposal |
| `core friday docs-dismiss` | INT-244 v22 Pillar 1 -- dismiss a doc proposal |

---

## core synthesize

Core v18 Synthesis Engine

| Command | Description |
|---------|-------------|
| `core synthesize now` | Generate synthesis snapshot now |
| `core synthesize brief` | Show current Friday brief |
| `core synthesize history` | Show synthesis history |

---

## core knowledge

Friday Knowledge Engine -- situated lessons

| Command | Description |
|---------|-------------|
| `core knowledge search` | Search knowledge base |
| `core knowledge patterns` | Show patterns by domain |
| `core knowledge accuracy` | Show accuracy by domain |
| `core knowledge add` | Add a lesson manually -- core knowledge add "fact" [domain] [resolution] |
| `core knowledge seed` | Seed forest lessons |
| `core knowledge show` | Show full entry detail |
| `core knowledge outcome` | Record outcome: core knowledge outcome <id> yes|no |

---

## core friday-arch

Friday Formal Architecture -- Meta-Interpretation Engine

| Command | Description |
|---------|-------------|
| `core friday-arch run` | Run full meta-interpretation cycle |
| `core friday-arch models` | Show Friday models and trust scores |
| `core friday-arch proposals` | Show pending proposals |
| `core friday-arch contradictions` | Show active contradictions |
| `core friday-arch trust-decay` | Run trust decay on all Friday models |
| `core friday-arch usefulness` | Show Friday usefulness metrics |
| `core friday-arch simulate` | Simulate a command -- predict outcome before execution |
| `core friday-arch simulate-accuracy` | Resolve simulation predictions against actual outcomes |
| `core friday-arch simulate-stats` | Show simulation accuracy stats |
| `core friday-arch propose` | Generate a new Friday proposal from current forest context |
| `core friday-arch approve` | Approve a pending Friday proposal |
| `core friday-arch reject` | Reject a pending Friday proposal |

---

## core version

*No subcommands.*

---

## core doctor

| Command | Description |
|---------|-------------|
| `core doctor run` | Run full health check |
| `core doctor aliases` | Audit aliases |
| `core doctor entropy` | Check configuration entropy/drift |
| `core doctor bins` | Check binary manifest drift |
| `core doctor trend` | Health trend analysis — pattern over time |
| `core doctor forecast` | Health forecast — predicted trajectory |
| `core doctor rebuild` | Generate deterministic rebuild plan |
| `core doctor quick` | Quick health check — critical checks only |
| `core doctor history` | Show health score history over time |

---

## core plugin

Manage ecosystem plugins

| Command | Description |
|---------|-------------|
| `core plugin list` | List all registered plugins |
| `core plugin add` | Register a plugin |
| `core plugin remove` | Remove a plugin from registry |
| `core plugin status` | Show plugin status |

---

## core link

| Command | Description |
|---------|-------------|
| `core link status` |  |
| `core link list` |  |
| `core link audit` |  |
| `core link plan` | Show what deploy would do without doing it |
| `core link deploy` | Deploy a package (create symlinks) |
| `core link undeploy` | Remove a package's symlinks |
| `core link adopt` | Undeploy then redeploy atomically Adopt existing files (convert real files to managed symlinks) |
| `core link redeploy` |  |
| `core link sync` | Sync all packages — deploy clean, surface conflicts with fix commands |

---

## core zone

*No subcommands.*

---

## core intent

| Command | Description |
|---------|-------------|
| `core intent focus` | Set focused intent |
| `core intent unfocus` | Clear focused intent |
| `core intent status` | Show current focus and workflow state |
| `core intent drift` | Detect if work has drifted from focused intent |
| `core intent start` | Transition intent to in-progress and set focus |
| `core intent complete` | Mark intent as complete and move to complete/ |
| `core intent new` | Create a new intent from template |
| `core intent deps` | Show dependency graph for an intent |
| `core intent burndown` | Show completion burndown chart |
| `core intent velocity` | Show velocity metrics |
| `core intent branch` | Generate git branch name for intent |
| `core intent list` |  |
| `core intent show` |  |
| `core intent search` |  |
| `core intent stats` |  |
| `core intent validate` |  |
| `core intent predict` | Predict completion date based on gate velocity |
| `core intent story` | Show rich narrative autobiography of an intent |
| `core intent auto-link` | Find related intents by tag overlap |
| `core intent health` | Show health scores for active intents |
| `core intent edit` | Open intent file in $EDITOR |
| `core intent blocked` | Show blocked intents and what is blocking them |
| `core intent next` | Recommend next intent with explanation |
| `core intent brief` | Session brief -- active intent, velocity, what to do |
| `core intent graph` | ASCII dependency graph of all intents |

---

## core profile

| Command | Description |
|---------|-------------|
| `core profile list` |  |
| `core profile status` |  |
| `core profile switch` |  |
| `core profile history` |  |
| `core profile health` |  |

---

## core security

| Command | Description |
|---------|-------------|
| `core security scan` |  |
| `core security debt` | Show debt score — how long each finding has been present |
| `core security trend` | Show finding count trend over time |
| `core security report` |  |
| `core security show` |  |
| `core security history` |  |
| `core security advise` | Security judgment advisory |
| `core security simulate` | Simulate applying a security patch or CVE fix |

---

## core sandbox

| Command | Description |
|---------|-------------|
| `core sandbox run` |  |
| `core sandbox diff` |  |
| `core sandbox status` |  |
| `core sandbox clear` |  |
| `core sandbox snapshot` |  |
| `core sandbox restore` |  |
| `core sandbox snapshots` |  |

---

## core fetch

*No subcommands.*

---

## core git

| Command | Description |
|---------|-------------|
| `core git status` |  |
| `core git risk` |  |
| `core git log` |  |
| `core git verify` |  |
| `core git commit` |  |
| `core git sync` |  |
| `core git quick` |  |
| `core git branch` |  |
| `core git install-hooks` |  |
| `core git remove-hooks` |  |

---

## core workspace

| Command | Description |
|---------|-------------|
| `core workspace view` |  |
| `core workspace recent` |  |
| `core workspace fm` |  |

---

## core release

| Command | Description |
|---------|-------------|
| `core release get` |  |
| `core release bump-tool` |  |
| `core release bump-system` |  |

---

## core notify

| Command | Description |
|---------|-------------|
| `core notify send` |  |
| `core notify status` |  |

---

## core lock

*No subcommands.*

---

## core launcher

| Command | Description |
|---------|-------------|
| `core launcher palette` |  |
| `core launcher dmenu` |  |
| `core launcher launch` |  |

---

## core update

| Command | Description |
|---------|-------------|
| `core update run` |  |
| `core update safe` |  |

---

## core weight

Core v17 — Pattern Weight Engine

| Command | Description |
|---------|-------------|
| `core weight list` | Show all patterns ranked by weight |
| `core weight top` | Show Critical and Strong patterns only |
| `core weight compute` | Scan events and compute pattern weights |
| `core weight explain` | Full weight breakdown for a pattern |
| `core weight calibrate` | Record outcome for calibration |

---

## core daemon

Query faelight-daemon v2 — background brain

| Command | Description |
|---------|-------------|
| `core daemon status` | Current daemon health and forest context |
| `core daemon context` | Full forest context snapshot from daemon |
| `core daemon signals` | Recent engine signals |
| `core daemon neovim` | Neovim context for a file |
| `core daemon watchdog` | Health watchdog status |

---

## core self

Core v16 — Self-Transformation: The Forest Redesigns Itself

| Command | Description |
|---------|-------------|
| `core self map` | Architecture coupling analysis |
| `core self evolve` | Generate structural proposals with confidence + risk |
| `core self apply` | Apply a proposal (use --dry-run first) |
| `core self history` | Evolution audit trail |
| `core self learn` | Record outcome of a proposal |
| `core self accuracy` | Proposal accuracy over time |
| `core self calibrate` | Adjust proposal thresholds based on history |
| `core self challenge` | Prove-me-wrong mode — stress test a plan |

---

## core journal

Forest journal — the system writes its own story

| Command | Description |
|---------|-------------|
| `core journal today` | Show today's journal entries |
| `core journal yesterday` | Show yesterday's journal entries |
| `core journal week` | Show this week's journal entries |
| `core journal search` | Search journal by keyword |
| `core journal show` | Show journal for a specific date (YYYY-MM-DD) |
| `core journal session-start` | Write a session-start entry |
| `core journal daily-summary` | Write a daily summary entry |

---

## core docs

Forest documentation — access guides and references

| Command | Description |
|---------|-------------|
| `core docs commands` | Show the core commands guide |
| `core docs list` | List available documentation |

---

## core values

Declared values system — define and manage your principles

| Command | Description |
|---------|-------------|
| `core values list` | List all declared values |
| `core values define` | Declare a new value |
| `core values remove` | Deactivate a declared value by ID |
| `core values weight` | Update the weight of a declared value |

---

## core align

Alignment checking — verify behavior matches declared values

| Command | Description |
|---------|-------------|
| `core align check` | Check alignment of a subject against declared values |
| `core align drift` | Show behavioral drift report for the last 30 days |
| `core align report` | Show weekly alignment report |

---

## core engines

Engine coordination layer — synchronize and monitor all forest engines

| Command | Description |
|---------|-------------|
| `core engines status` | Show all engines and their sync state |
| `core engines sync` | Acknowledge engine upgrade and update contracts |
| `core engines signals` | Show recent cross-engine signals |
| `core engines check` | Verify all engines are consistent |
| `core engines upgrade-log` | Show engine upgrade history |
| `core engines process` | Process unconsumed signals and route reactions |

---

## core events

Query the event ledger

| Command | Description |
|---------|-------------|
| `core events status` | Show event log file status and size |
| `core events archive` | Archive and compress old log files |
| `core events list` | List events from today |
| `core events since` | Events since a duration (e.g. 1h, 30m, 2d) |
| `core events filter` | Filter events by domain |
| `core events watch` | Live event stream — watch events as they happen |
| `core events emit-v2` | Emit a validated signal to forest_events_v2 |
| `core events replay` | Replay signals from sequence range |
| `core events chain` | Show causality chain for a signal |
| `core events status-v2` | Show forest_events_v2 status |

---

## core ledger

Ledger analytics — query and export the event ledger

| Command | Description |
|---------|-------------|
| `core ledger stats` | Event counts, domains, date range, and database stats |
| `core ledger query` | Query events for a specific domain |
| `core ledger export` | Export all events to JSON |
| `core ledger indexes` | Create database indexes for fast time-window queries |

---

## core why

Causality engine — why is the system in this state?

| Command | Description |
|---------|-------------|
| `core why summary` | Why did the system do what it did today? |
| `core why health` | Why is health at its current level? |
| `core why domain` | What has a specific domain been doing? |
| `core why visual` | Visual topology — what apps and workspaces were active today? |
| `core why attention` | Attention analysis — focus quality and fragmentation? |
| `core why health-since` | Health trajectory since a specific date (YYYY-MM-DD) |
| `core why causal` | What events in a domain preceded health changes? |
| `core why chain` | Full causal chain for last health drop |
| `core why correlate` | Correlate two domains — find patterns between them |
| `core why suggest` | Proactive suggestions based on current state and learned patterns |
| `core why workspace` | Visual workspace activity — what drove workspace switches? |
| `core why focus` | Focus quality — deep focus vs fragmentation over time |

---

## core trace

Trace event history with full detail

| Command | Description |
|---------|-------------|
| `core trace last` | Show last 10 events with full detail |
| `core trace domain` | Show full trace for a specific domain |

---

## core goals

Goal engine — the forest chooses where to grow (Core v9)

| Command | Description |
|---------|-------------|
| `core goals list` | List all active forest goals |
| `core goals generate` | Generate new goals from current forest evidence |
| `core goals priority` | Ranked goal list with reasoning |
| `core goals accept` | Accept a goal — becomes an intent record |
| `core goals reject` | Reject a goal — logged with reason |
| `core goals show` | Show detail for a specific goal |

---

## core stress

Stress test engine — verify v11 before v12 builds on top (INT-152)

| Command | Description |
|---------|-------------|
| `core stress events` | Event storm — inject 500 events, verify no corruption |
| `core stress predict` | Prediction under load — all 9 predict commands |
| `core stress react` | Reaction integrity — cooldowns and history |
| `core stress health` | Health trajectory integrity |
| `core stress intents` | Intent velocity accuracy |
| `core stress report` | Full stress report — run all tests |
| `core stress health-report` | Chaos health report — all 5 scenarios |
| `core stress scenario1` | Scenario 1 — sudden health drop |
| `core stress scenario2` | Scenario 2 — slow decline detection |
| `core stress scenario3` | Scenario 3 — recovery verification |
| `core stress scenario4` | Scenario 4 — false alarm resistance |
| `core stress scenario5` | Scenario 5 — lock/unlock cycle |

---

## core predict

Prediction engine — the forest anticipates (Core v11)

| Command | Description |
|---------|-------------|
| `core predict sessions` |  |
| `core predict cadence` |  |
| `core predict health` |  |
| `core predict decline` |  |
| `core predict intents` | Estimated completion dates for planned intents |
| `core predict next` | What intent is most likely to ship next |
| `core predict coupling` | Coupling forecast — which domains will hit critical coupling |
| `core predict churn` | File churn prediction — highest risk files |
| `core predict accuracy` | Prediction confidence and accuracy tracking |
| `core predict verify` | Verify a prediction as correct or incorrect |
| `core predict why` | Explain why an intent is or is not predicted |
| `core predict cross-session` | Cross-session pattern analysis |
| `core predict memory-decay` | Memory decay — prune stale state.db entries |

---

## core react

Reaction engine — the forest responds without being asked (Core v10)

| Command | Description |
|---------|-------------|
| `core react list` | List all reaction rules and their cooldown status |
| `core react rules` | Show full rule registry with TOML overrides applied |
| `core react run` | Evaluate all rules now and surface any reactions |
| `core react history` | Show log of all triggered reactions |
| `core react explain` | Explain why a specific reaction fired |
| `core react discipline` | Show active cooldown timers |
| `core react enable` | Enable a reaction rule |
| `core react disable` | Disable a reaction rule |
| `core react bounds` | Show current reaction boundary gates and health status |
| `core react audit` | Audit all rules against current boundaries and goals |
| `core react story` | Show today's reaction narrative as a story |
| `core react coalesce` | Show batched coalescing signal groups |
| `core react discipline-show` | Show discipline config — decay, coalesce, escalate settings |
| `core react add` | Add a custom reaction rule |

---

## core strategy

Strategy engine — the forest plans across horizons (Core v12)

| Command | Description |
|---------|-------------|
| `core strategy now` | What needs attention this session? |
| `core strategy week` | What should the next 7 days focus on? |
| `core strategy quarter` | The 90-day arc toward Jarvis |
| `core strategy sequence` | Optimal path to achieve a goal |
| `core strategy unblock` | What is blocking the most progress? |
| `core strategy tradeoff` | What do we give up to do this action now? |
| `core strategy conflicts` | Which intents are pulling in opposite directions? |
| `core strategy coherence` | Is the current work plan internally consistent? |
| `core strategy merge` | Can these two goals be pursued together? |
| `core strategy jarvis` | How close is the forest to Jarvis-level capability? |
| `core strategy trust` | What evidence would justify more autonomy? |
| `core strategy gap` | What capabilities are missing for full Jarvis? |
| `core strategy history` | Past strategies and did they help? |
| `core strategy learn` | Record outcome of a strategy |
| `core strategy review` | What worked, what didn't? |
| `core strategy next` | What should I work on next? (INT-181) |
| `core strategy queue` | Ordered work queue for next 5 sessions (INT-181) |
| `core strategy blockers` | What is blocking the most planned intents? (INT-181) |

---

## core db

Database backup and recovery (INT-166)

| Command | Description |
|---------|-------------|
| `core db backup` | Manual snapshot to timestamped file |
| `core db restore` | Restore from a backup snapshot |
| `core db verify` | Run integrity check |
| `core db status` | Show db size, table counts, last backup |
| `core db compact` | VACUUM to reclaim space |

---

## core genealogy

Genealogy — the forest remembers how it grew (INT-153)

| Command | Description |
|---------|-------------|
| `core genealogy show` | Show lineage of a specific intent |
| `core genealogy tree` | Show full intent family tree |
| `core genealogy roots` | Show founding intents with no ancestors |

---

## core integrity

Integrity — consistency oracle and self-repair engine (INT-184)

| Command | Description |
|---------|-------------|
| `core integrity run` | Full integrity scan with repair options |
| `core integrity status` | Show current integrity status and pending proposals |
| `core integrity log` | Show history of integrity issues |
| `core integrity fix` | Apply pending proposals |
| `core integrity apply` | Apply a specific pending proposal by ID |
| `core integrity heal` | Auto-heal all safe integrity issues |
| `core integrity trend` | Show integrity score trend over time |

---

## core autonomy

Autonomy — mandate system and autonomous action engine (INT-156)

| Command | Description |
|---------|-------------|
| `core autonomy mandate-list` | List active mandates |
| `core autonomy mandate-set` | Define a new mandate |
| `core autonomy mandate-revoke` | Revoke a mandate by ID |
| `core autonomy mandate-revoke-all` | Revoke all mandates — return to manual mode |
| `core autonomy pending` | Show pending autonomous actions |
| `core autonomy run` | Execute pending authorized actions |
| `core autonomy log` | Show autonomy action log |
| `core autonomy revert` | Revert last autonomous action |
| `core autonomy trust-score` | Show trust score |
| `core autonomy trust-history` | Show trust history |
| `core autonomy trust-expand` | Request expanded autonomy |

---

## core partner

Partner — collaborative intent creation and shared decision making (v14)

| Command | Description |
|---------|-------------|
| `core partner propose` | Forest proposes a new intent based on observed patterns |
| `core partner discuss` | Forest shares opinion on an existing intent |
| `core partner disagree` | Forest respectfully pushes back on an intent |
| `core partner consult` | Consult the forest before making a decision |
| `core partner reflect` | What has the forest learned about your work style? |
| `core partner pattern` | What patterns define how you work? |
| `core partner growth` | How has the system grown over time? |
| `core partner pushback` | Show recent pushback moments |
| `core partner roadmap` | Forest view of the optimal path forward |
| `core partner roadmap-why` | Why does the forest recommend this roadmap? |
| `core partner roadmap-diff` | How does forest roadmap differ from current plan? |
| `core partner status` | Partner system status and readiness |

---

## core registry

Registry — manage the tool registry (INT-183)

| Command | Description |
|---------|-------------|
| `core registry list` | List all tools in the registry |
| `core registry show` | Show details for a specific tool |
| `core registry retire` | Retire a tool — mark as retired, excluded from deploy and doctor |
| `core registry unretire` | Unretire a tool — restore to active status |
| `core registry reality-check` | Compare actual tool usage vs expected_usage |

---

## core plan

Task planning — break accepted goals into concrete steps (Core v9)

| Command | Description |
|---------|-------------|
| `core plan generate` | Generate a task plan for an accepted goal |
| `core plan review` | Review a plan in full |
| `core plan simulate` | Simulate plan execution — risk analysis via scenario engine |
| `core plan list` | List all plans |

---

## core tradeoff

Tradeoff engine — surface competing values in decisions (Core v9)

| Command | Description |
|---------|-------------|
| `core tradeoff analyze` | Analyze competing values for a decision |
| `core tradeoff history` | Show past tradeoff analyses |
| `core tradeoff balance` | Current system balance across all four axes |

---

## core prioritize

Dynamic prioritization — rerank goals by live conditions (Core v9)

| Command | Description |
|---------|-------------|
| `core prioritize run` | Rerank all goals given current forest state |
| `core prioritize explain` | Explain why goals are ranked as they are |

---

## core autobiography

Intent autobiography — the forest narrates its own goal history (Core v9)

| Command | Description |
|---------|-------------|
| `core autobiography narrate` | Narrate the forest's goal history |

---

## core evolution

| Command | Description |
|---------|-------------|
| `core evolution map` | Architecture map — domain structure, file counts, coupling index |
| `core evolution tools` | Tools usage analysis — roster, age, lifecycle stage |
| `core evolution suggest` | Architecture suggestions based on coupling, churn and tool data |
| `core evolution evolve-propose` | Generate a formal evolution proposal from suggestions |
| `core evolution evolve-list` | List all evolution proposals |
| `core evolution evolve-accept` | Accept a proposal — creates an intent record |
| `core evolution evolve-reject` | Reject a proposal — logged with reason |
| `core evolution future-sim` | Simulate an architectural change — what would break? |
| `core evolution future-risk` | Risk analysis for a proposed architectural change |
| `core evolution future-impact` | Impact analysis — which domains/tools are affected? |

---

## core simulate

| Command | Description |
|---------|-------------|
| `core simulate doctor` | Predict health after pending changes — no writes |
| `core simulate update` | Show what packages would be updated — no writes |
| `core simulate scenario` | Simulate risk for a planned scenario using decision history |

---

## core delegate

Delegation engine — trust contracts and safe autonomy simulation

| Command | Description |
|---------|-------------|
| `core delegate simulate` | Simulate a delegation action without executing |
| `core delegate contracts` | List all trust contracts and their status |
| `core delegate history` | Show delegation simulation history |
| `core delegate accuracy` | Show simulation accuracy over time |
| `core delegate suspend` | Suspend all delegation instantly |
| `core delegate activate` | Activate a contract after gate is met |
| `core delegate counterfactuals` | Show counterfactual comparison log |
| `core delegate log-counterfactual` | Log a counterfactual (proposed vs actual action) |
| `core delegate accuracy-report` | Three-dimensional accuracy report |

---

## core checkpoint

Checkpoint system state for recovery

| Command | Description |
|---------|-------------|
| `core checkpoint restore` | Restore config files from a named checkpoint |
| `core checkpoint last-good` | Find and restore last checkpoint with 95%+ health |
| `core checkpoint snapshot` | Create a btrfs snapshot of @home |
| `core checkpoint snapshots` | List btrfs snapshots |
| `core checkpoint create` | Create a named checkpoint of current system state |
| `core checkpoint list` | List all checkpoints |
| `core checkpoint diff` | Show what changed since a checkpoint |

---

## core decision

Show capability requirements for all domains Record and track decisions with context snapshots

| Command | Description |
|---------|-------------|
| `core decision record` | Record a new decision with context snapshot |
| `core decision outcome` | Record the outcome of a decision |
| `core decision list` | List recorded decisions |
| `core decision hindsight` | View hindsight summary of all decisions |
| `core decision show` | Show full detail for a specific decision |
| `core decision stats` | Correlation stats across all decisions |
| `core decision advise` | Judgment advisory for current state |
| `core decision heuristics` | Auto-derived heuristics from decision corpus |
| `core decision lessons` | Human-readable lessons summary |
| `core decision story` | 30-day narrative of computing life |
| `core decision patterns` | Detect repeating decision patterns |
| `core decision friction` | Detect decisions requiring repeated corrections |
| `core decision reversal` | Detect architectural reversals in decision history |

---

## core heuristics

Auto-derived heuristics from decision corpus

*No subcommands.*

---

## core deps

Dependency intelligence

| Command | Description |
|---------|-------------|
| `core deps graph` | Visual dependency map of all forest tools |
| `core deps risk` | Which dependencies carry the most risk? |
| `core deps audit` | Cross-reference deps with decision history |

---

## core narrative

Forest narrative — the story of how the forest became what it is

*No subcommands.*

---

## core snapshot

Snapshot narrative — the forest writes its own autobiography

*No subcommands.*

---

## core bootstrap

Bootstrap intelligence — rebuild guidance

| Command | Description |
|---------|-------------|
| `core bootstrap plan` | Generate reconstruction plan |
| `core bootstrap verify` | Verify current state consistency |
| `core bootstrap diff` | Show what diverged from canonical state |

---

## core anomaly

Detect unexpected system changes

| Command | Description |
|---------|-------------|
| `core anomaly scan` | Detect unexpected system changes |
| `core anomaly history` | Show anomaly detection history |
| `core anomaly alert` | Surface high-severity anomalies |

---

## core audit

Audit tool health and intelligence

| Command | Description |
|---------|-------------|
| `core audit scan` | Score all tools — full intelligence report |
| `core audit show` | Deep audit of a specific tool |
| `core audit stale` | Tools below health threshold |
| `core audit coverage` | Tools missing documentation |

---

## core lessons

What the forest has learned — heuristics summary

*No subcommands.*

---

## core story

The forest narrative — 30 day story

*No subcommands.*

---

## core advise

Judgment advisory for current system state

*No subcommands.*

---

## core decide

Record a decision directly (shorthand for decision record)

*No subcommands.*

---

## core hindsight

View decision hindsight summary

*No subcommands.*

---

## core capabilities

*No subcommands.*

---

*Generated by `docs command-guide` -- never edit by hand*
