---
id: 211
date: 2026-08-09
type: future
title: "the intent ledger has no canonical document shape -- "gate" has four spellings, "date" means four different things, and only two intents record an outcome"
status: planned
tags: [ledger, intent, schema]
---

## Vision
[Describe the goal and desired outcome]

## The Problem
[What problem does this solve?]

## The Solution
[High-level approach]

## Success Criteria
- [ ] ...

<!-- ⚠️ RETURNED TO PLANNED 2026-09-04. This was never written.

Filed 2026-08-09 as a template and left as one: every section still holds its placeholder,
the only gate is `- [ ] ...`, and the boilerplate comment that says delete this when the
intent is written is still here. At some point cistart moved it to in-progress, so the
ledger claimed work was underway on an unspecified document for four weeks.

⭐ THAT IS ITSELF THE FINDING, and it belongs to INT-232: the ledger cannot mechanically
answer whether an intent is done, and it cannot answer whether one has been STARTED either.
An empty template and a fully specified intent look identical to cistart.

NOT CANCELLED, because the subject is live. Today alone: gates were ticked [x], ruled void
with an invented [~], and one intent carried two separate criteria lists from different
eras. That is a fifth spelling of gate added in a single session, by the person writing
this. The problem this intent names is real and getting worse.

It needs writing before it needs working. -->

<!-- INT-158 -- EVIDENCE CONVENTION. A ticked box is a promise. Evidence is the receipt.
When you tick a gate, put the proof in an HTML comment on the line after it: a commit
hash, a file:line, a log or artifact path, or "demonstrated: what + how". Prose counts.
FORWARD-ONLY (never retrofit old intents -- busywork, no payoff).
SOFT (a discipline, not gate-police; nothing enforces this).
LIGHT (trivial self-evident gates need no artifact).
Exemplars: INT-133 (the original), INT-161, INT-112, INT-061.
See docs/CONVENTIONS.md. Delete this comment when the intent is written. -->
