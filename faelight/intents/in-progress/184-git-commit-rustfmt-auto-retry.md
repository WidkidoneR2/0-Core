---
id: 184
date: 2026-07-20
type: improvement
title: "git commit: rustfmt auto-stages at commit time (kills the reformat-then-fail trap)"
status: in-progress
tags: [fsh, git, workflow, rustfmt, tooling]
---

## Vision
Committing Rust changes lands on the first try. The rustfmt pre-commit hook reformatting a file
should not silently cost you the commit.

## The Problem
OBSERVED four times 2026-07-20: `git commit` on a .rs change triggers the rustfmt pre-commit
hook, which REFORMATS the file and then FAILS the commit ("files were modified by this hook").
The commit does NOT land. If you then proceed (e.g. `dep`), you build/deploy from an UNCOMMITTED
tree -- a dirty-tree advisory, and work that looks committed but is not. The fix each time was
manual: re-add the now-reformatted file and commit again. It works, but it is a silent trap that
bit us repeatedly, and it is exactly the kind of papercut that wastes attention on every Rust
commit.

## The Solution
Make the commit flow rustfmt-aware. When a commit fails specifically because rustfmt reformatted
tracked files (hook id rustfmt, "files were modified by this hook"), automatically re-stage the
reformatted files and re-run the commit ONCE, then report the result. Options (choose after
recon):
- A wrapper/alias around commit (the `gp`/commit path) that detects the rustfmt-modified failure
  and retries.
- Or configure the hook to stage its own reformats (if the pre-commit framework supports it).
Guardrails: retry ONCE only (avoid loops); only auto-retry for the rustfmt-reformatted case, not
for real failures (ripsecrets/risk-gate failures must still stop the commit and surface loudly);
never auto-commit something the human did not already ask to commit.

## Success Criteria
- [x] Recon: how the commit path and the pre-commit hooks are wired (the rustfmt hook, the
      <!-- DONE 2026-07-21. faelight-git commit uses libgit2 (repo.rs:342 self.repo.commit) which
BYPASSES pre-commit hooks entirely; gp is a fsh builtin (main.rs:1142). The rustfmt trap fires ONLY
on raw `git commit` (what we type), which hits .git/hooks/pre-commit. Config is generated by
git-hooks.nix from flake.nix:294 (rustfmt.enable=true). .pre-commit-config.yaml is "# DO NOT MODIFY".
Hooks: rustfmt, ripsecrets, risk-gate. Installed via devShell shellHook (${preCommitHook}, INT-119). -->
      commit alias/flow). Documented.
- [x] The rustfmt-reformatted-and-failed case is detected specifically (not confused with real
      <!-- APPROACH CHANGED (better): rather than DETECT-then-retry the failure, we eliminated the
failure at the source. Recon showed the trap is stock cachix/git-hooks.nix rustfmt behavior (reformat
-> "files were modified" -> fail). Instead of catching that, we made rustfmt AUTO-STAGE so it never
fails. No detection needed -- the benign case simply stopped existing. Real failures still handled by
their own hooks (see below). -->
      hook failures like ripsecrets/risk-gate).
- [x] On that case only, the reformatted files are re-staged and the commit retried ONCE; the
      <!-- DONE (via auto-stage, not retry) 2026-07-21, commit 07fd0193. Replaced flake.nix
`rustfmt.enable = true` with a custom `rustfmt-autostage` hook: pkgs.writeShellScript running
`cargo fmt --all` then `git add -u`. rustfmt reformats AND stages, exits 0 -> commit lands FIRST-TRY
with formatted code, no retry. PROVEN with a VALID test: appended a mangled-but-valid mod-declared fn
(int184_fmt_probe) to exec.rs, committed -> landed first-try (a37e7d98), git show HEAD showed it
FORMATTED (fn int184_fmt_probe() -> i32 { let x = 1; ... }). ⚠️ First test was INVALID: an orphan .rs
in src/ that nothing mod-declares -- cargo fmt --all never sees it (only formats module-tree-reachable
files), so it committed unformatted. That is CORRECT behavior for non-code; the valid test used a real
mod-declared file. ⚠️ pre-commit STASHES unstaged/runs hooks on staged tree/restores -- concern was the
hook's `git add -u` might not survive; the valid test PROVED it does (reformatting landed in the commit). -->
      commit lands. Demonstrated on a real .rs change that rustfmt reformats.
- [x] Real failures (secrets, risk-gate) still STOP the commit and surface -- proven, no silent
      <!-- DONE 2026-07-21. ripsecrets and risk-gate are UNCHANGED, separate hooks -- the flake.nix
edit only touched the rustfmt hook. Every test commit this session showed "ripsecrets...Passed
risk-gate...Passed" running normally; a real secret or risk-gate violation still fails the commit
exactly as before. The autostage change is scoped to rustfmt only; it cannot swallow a secrets/risk
failure (different hooks, run independently by pre-commit). -->
      swallow.
- [x] No loops (retry is once), no committing of unrequested content.
      <!-- DONE 2026-07-21. No retry loop exists at all -- the commit succeeds on the first attempt
(no retry mechanism to loop). "Unrequested content": `git add -u` stages only TRACKED files that
rustfmt modified (the .rs files already part of the commit) -- it does NOT add untracked files (-u
excludes them) and does not touch files outside the reformatting. For the whole-file staging workflow
used here this is exactly right. Noted footgun for the record: if one ever partially-staged hunks of a
.rs file, `git add -u` would stage the whole file's reformatting -- not a concern for this workflow. -->
- [x] Each gate carries evidence per INT-158.
      <!-- DONE 2026-07-21. Commits 07fd0193 (flake.nix fix) + a37e7d98 (valid test, since amended
out) + 6f419289 (probe cleanup). nix flake check "all checks passed!" (Nix valid, boot test intact).
Hook regen proven: `nix develop --command true` -> "pre-commit installed at .git/hooks/pre-commit",
.pre-commit-config.yaml now shows rustfmt-autostage. INT-119 discipline honored: proved a real commit
auto-formats, did not trust the config. -->

## The Rule
"A formatter fixing your whitespace should never cost you the commit. Detect the one benign
failure, re-add, retry once -- and let every real failure still shout." 🌲
