[package]
name = "faelight-vm"
version = "0.1.0"
edition.workspace = true
authors.workspace = true
license.workspace = true

[[bin]]
name = "faelight-vm"
path = "src/main.rs"

[dependencies]
# INT-256: libc for the broken-pipe signal only -- see main.rs. NOT faelight-core: one
# signal call does not justify carrying the ledger crate.
libc = "0.2"
