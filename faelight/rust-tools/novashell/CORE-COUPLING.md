# INT-230 G1 -- novashell coupling to faelight_core, by capability

Generated by `census-core-coupling.py`. Re-run and diff; do not hand-edit.

## Units

Three different numbers get confused here, so all three are stated:

- lines mentioning `faelight_core`: **45**
- classified `paths::<fn>` calls: **48**
- bare `paths::` with no function (use statements): **3**

A single line can do more than one of these. The unit G1 counts is the
classified call.

## What this census CANNOT see

Recorded so a clean census is never read as a clean shell.

- It matches **text, not syntax**, so a comment mentioning a
  `paths::` function counts as a call (`nl.rs:460`).
- It only finds **`paths::` calls**. Thirty hand-built `0-core/`
  paths exist in this shell and are invisible here, twelve of them
  pointing at `~/0-core/scripts`, which does not exist. INT-240.
- It classifies by **function**, so one function asked two different
  questions cannot be split -- `intents_dir` serves both discovery
  and the catastrophic-rm guard. The evidence list below is where a
  reader sees which call is which.
- It measures **path coupling, not the defect class**. A fabricated
  default read from the database rather than a path is invisible to
  it (`commands/mod.rs:6776`, `db.health_score().unwrap_or(0)`).

## Summary

| bucket | calls | functions |
| --- | --- | --- |
| core shell state | 35 | bin_dir, daemon_socket, home, runtime_dir, shell_config, state_db, state_home |
| 0-Core discovery | 9 | core_root_string, intents_dir, registry_dir, rust_tools_dir |
| 0-Core observability | 4 | changelog_file, read_health, version_file |
| 0-Core UI enrichment | 0 | (split by caller -- see G2) |

## Evidence

### core shell state

**paths::bin_dir** -- 2 calls

- `exec.rs:350`
- `registry.rs:186`

**paths::daemon_socket** -- 3 calls

- `engine.rs:275`
- `engine.rs:540`
- `main.rs:3629`

**paths::home** -- 1 calls

- `exec.rs:353`

**paths::runtime_dir** -- 1 calls

- `main.rs:2179`

**paths::shell_config** -- 5 calls

- `cheatsheet_tui.rs:199`
- `cheatsheet_tui.rs:743`
- `commands/mod.rs:4878`
- `config.rs:76`
- `config.rs:78`

**paths::state_db** -- 22 calls

- `cheatsheet_tui.rs:356`
- `commands/mod.rs:1976`
- `commands/mod.rs:2149`
- `commands/mod.rs:2180`
- `commands/mod.rs:2230`
- `commands/mod.rs:2282`
- `commands/mod.rs:2325`
- `commands/mod.rs:2403`
- `commands/mod.rs:2492`
- `commands/mod.rs:2525`
- `commands/mod.rs:12514`
- `commands/mod.rs:12522`
- `commands/mod.rs:14811`
- `commands/mod.rs:15827`
- `completion.rs:1151`
- `db.rs:69`
- `history_tui.rs:57`
- `main.rs:984`
- `main.rs:2178`
- `main.rs:2675`
- `prompt.rs:492`
- `safety_guard.rs:185`

**paths::state_home** -- 1 calls

- `core_integration.rs:153`

### 0-Core discovery

**paths::core_root_string** -- 1 calls

- `db.rs:68`

**paths::intents_dir** -- 4 calls

- `core_integration.rs:180`
- `exec.rs:311`
- `exec.rs:1543`
- `main.rs:3126`

**paths::registry_dir** -- 2 calls

- `core_integration.rs:190`
- `nl.rs:460`

**paths::rust_tools_dir** -- 2 calls

- `cheatsheet_tui.rs:265`
- `core_integration.rs:51`

### 0-Core observability

**paths::changelog_file** -- 1 calls

- `core_integration.rs:110`

**paths::read_health** -- 2 calls

- `core_integration.rs:128`
- `core_integration.rs:134`

**paths::version_file** -- 1 calls

- `core_integration.rs:93`

### bare paths:: (use statements, not calls)

- `core_integration.rs:5` -- //! WHY THIS EXISTS. The G1 census (CORE-COUPLING.md) measured 82 `paths::` calls
- `core_integration.rs:150` -- /// asked `paths.rs` -- so the census, which only finds `paths::` calls, could
- `exec.rs:295` -- // that happens to apply under the same condition, and it depends on core_root and the paths::

