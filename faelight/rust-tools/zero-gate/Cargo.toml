[package]
name = "zero-gate"
version = "0.1.0"
edition = "2021"

# ⚠️ NO DEPENDENCIES, AND THAT IS THE POINT. This runs from a git hook on every commit, so it must
# start instantly and must not need the project it is guarding. It deliberately does NOT depend on
# faelight-core: that crate carries 0-Core's directory layout, and a gate that needs an intent
# ledger to run `cargo fmt` is carrying a house to hold a door open.
[dependencies]
