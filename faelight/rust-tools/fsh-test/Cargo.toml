[package]
name = "fsh-test"
version = "1.0.0"
edition = "2021"
license = "MIT"
description = "fsh permanent regression suite -- INT-304"

[dependencies]
faelight-core = { path = "../faelight-core" }
rusqlite = { version = "0.31", features = ["bundled"] }
colored = "2"
chrono = "0.4"
