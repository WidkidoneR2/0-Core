# INT-112: risk metadata.
risk = "user"
reason = """
Home Manager: shell config, terminal, git, the bar, notify. INT-061 decision #5's user side
of the authority boundary.
NOT lockout-class, and this was MEASURED rather than assumed (2026-07-16): the christian
account's login shell is bash --
    getent passwd christian -> /run/current-system/sw/bin/bash
    nix eval ...users.christian.shell.name -> bash-interactive-5.3p9
So fsh is the TERMINAL, not the login. A bad fsh build costs a shell prompt, not a machine.
If a `shell = pkgs.faelight-shell` line ever appears in framework16's user block, this dir
and faelight/rust-tools/faelight-shell/ both become critical. It is not there today.
"""
