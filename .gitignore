.direnv/
target/
result
result-*
runtime/
faelight-vm.qcow2
*.bak-*

# INT-119: generated by git-hooks.nix from flake.nix on devShell entry. Not source.
.pre-commit-config.yaml
__pycache__/

# INT-206 STOPGAP -- DELETE THIS BLOCK WHEN 206 LANDS.
# Two conformance cases execute as redirects and write files named 0.5 and = wherever the shell runs.
# fsh-test asks for /tmp, but fsh overrides the directory it was spawned in and starts in the forest
# home instead, so they land here on every suite run. These files have been committed by accident
# before. Anchored to the repo root: a nested file with either name is not affected.
# ⚠️ This HIDES the symptom. The fix is INT-206, and this block goes with it.
/0.5
/=
