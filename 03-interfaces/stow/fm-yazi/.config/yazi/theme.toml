# ═══════════════════════════════════════════════════════════
# 🌲 Faelight Forest Yazi Theme v8.0.0 - ENHANCED
# ──────────────────────────────────────────────────────────
# NEON highlights for stow packages & important files
# Smart color coding by file importance
# ═══════════════════════════════════════════════════════════

[flavor]
use = ""

[mgr]
cwd = { fg = "#8ed1a3" }

hovered         = { fg = "#0f1c16", bg = "#5bb7a5" }
preview_hovered = { underline = true }

find_keyword  = { fg = "#c7df63", italic = true }
find_position = { fg = "#8ed1a3", bg = "reset", italic = true }

marker_selected = { fg = "#c7df63", bg = "#c7df63" }
marker_copied   = { fg = "#8ed1a3", bg = "#8ed1a3" }
marker_cut      = { fg = "#ff6b6b", bg = "#ff6b6b" }

tab_active   = { fg = "#0f1c16", bg = "#5bb7a5", bold = true }
tab_inactive = { fg = "#8ed1a3", bg = "#2e6146", bold = true }
tab_width    = 1

border_symbol = "│"
border_style  = { fg = "#5bb7a5" }

[status]
separator_open  = ""
separator_close = ""
separator_style = { fg = "#2e6146", bg = "#2e6146" }

mode_normal = { fg = "#0f1c16", bg = "#5bb7a5", bold = true }
mode_select = { fg = "#0f1c16", bg = "#c7df63", bold = true }
mode_unset  = { fg = "#0f1c16", bg = "#8ed1a3", bold = true }

progress_label  = { fg = "#e8f5d5", bold = true }
progress_normal = { fg = "#5bb7a5", bg = "#2e6146" }
progress_error  = { fg = "#ff6b6b", bg = "#2e6146" }

permissions_t = { fg = "#8ed1a3" }
permissions_r = { fg = "#c7df63" }
permissions_w = { fg = "#ff6b6b" }
permissions_x = { fg = "#5bb7a5" }
permissions_s = { fg = "#8ed1a3" }

# ═══════════════════════════════════════════════════════════
# 🎨 ENHANCED FILETYPE RULES - NEON COLOR CODING
# ═══════════════════════════════════════════════════════════
[filetype]
rules = [
    # ──────────────────────────────────────────────
    # 🌟 CRITICAL FILES - NEON RED
    # ──────────────────────────────────────────────
    { mime = "text/markdown", fg = "#ff3366", bold = true },
    { mime = "text/x-shellscript", fg = "#ff3366", bold = true },
    
    # ──────────────────────────────────────────────
    # 📦 STOW PACKAGES - NEON CYAN (BRIGHT!)
    # ──────────────────────────────────────────────
    { url = "shell-zsh/", fg = "#00ffff", bold = true },
    { url = "editor-nvim/", fg = "#00ffff", bold = true },
    { url = "term-foot/", fg = "#00ffff", bold = true },
    { url = "fm-yazi/", fg = "#00ffff", bold = true },
    { url = "browser-qutebrowser/", fg = "#00ffff", bold = true },
    { url = "shell-nushell/", fg = "#00ffff", bold = true },
    { url = "config-faelight/", fg = "#00ffff", bold = true },
    { url = "prompt-starship/", fg = "#00ffff", bold = true },
    { url = "tools-topgrade/", fg = "#00ffff", bold = true },
    { url = "tools-gitleaks/", fg = "#00ffff", bold = true },
    { url = "tools-direnv/", fg = "#00ffff", bold = true },
    
    # ──────────────────────────────────────────────
    # 📚 IMPORTANT DOCS - NEON MAGENTA
    # ──────────────────────────────────────────────
    { url = "README.md", fg = "#ff00ff", bold = true },
    { url = "CHANGELOG*.md", fg = "#ff00ff", bold = true },
    { url = "THEORY_OF_OPERATION.md", fg = "#ff00ff", bold = true },
    { url = "INTENT/", fg = "#ff00ff", bold = true },
    
    # ──────────────────────────────────────────────
    # 🔧 SCRIPTS & TOOLS - NEON ORANGE
    # ──────────────────────────────────────────────
    { url = "scripts/", fg = "#ff9933", bold = true },
    { url = "rust-tools/", fg = "#ff9933", bold = true },
    { url = "*.sh", fg = "#ff9933", bold = true },
    
    # ──────────────────────────────────────────────
    # ⚙️  CONFIG FILES - NEON YELLOW
    # ──────────────────────────────────────────────
    { url = "*.toml", fg = "#ffff00", bold = true },
    { url = "*.yaml", fg = "#ffff00", bold = true },
    { url = "*.yml", fg = "#ffff00", bold = true },
    { url = "*.conf", fg = "#ffff00", bold = true },
    { url = "Cargo.toml", fg = "#ffff00", bold = true },
    
    # ──────────────────────────────────────────────
    # 🔗 SYMLINKS - BRIGHT MAGENTA
    # ──────────────────────────────────────────────
    { url = "*", is = "link", fg = "#ff66ff", bold = true },
    
    # ──────────────────────────────────────────────
    # 🎯 RUST CODE - NEON GREEN
    # ──────────────────────────────────────────────
    { url = "*.rs", fg = "#00ff00" },
    { url = "Cargo.lock", fg = "#00ff00" },
    
    # ──────────────────────────────────────────────
    # 📄 MARKDOWN - SOFT GREEN
    # ──────────────────────────────────────────────
    { url = "*.md", fg = "#8ed1a3" },
    
    # ──────────────────────────────────────────────
    # 📁 DIRECTORIES - STANDARD GREEN
    # ──────────────────────────────────────────────
    { url = "*/", fg = "#8ed1a3" },
    
    # ──────────────────────────────────────────────
    # 🔒 HIDDEN/DOTFILES - YELLOW
    # ──────────────────────────────────────────────
    { url = ".*", fg = "#c7df63", bold = true },
    
    # ──────────────────────────────────────────────
    # ⚡ EXECUTABLES - TEAL
    # ──────────────────────────────────────────────
    { url = "*", is = "exec", fg = "#5bb7a5" },
    
    # ──────────────────────────────────────────────
    # 📄 DEFAULT FILES - STANDARD
    # ──────────────────────────────────────────────
    { url = "*", fg = "#e8f5d5" }
]

# ═══════════════════════════════════════════════════════════
# 🎨 ENHANCED ICON RULES
# ═══════════════════════════════════════════════════════════
[icon]
rules = [
    # ──────────────────────────────────────────────
    # 🌟 CRITICAL FILES
    # ──────────────────────────────────────────────
    { url = "PHILOSOPHY.md", text = "📜" },
    { url = "COMPLETE_GUIDE.md", text = "📖" },
    { url = ".envrc", text = "🔐" },
    { url = ".gitleaks.toml", text = "🛡️" },
    
    # ──────────────────────────────────────────────
    # 📦 STOW PACKAGES
    # ──────────────────────────────────────────────
    { url = "shell-zsh/", text = "🐚" },
    { url = "editor-nvim/", text = "📝" },
    { url = "term-foot/", text = "💻" },
    { url = "fm-yazi/", text = "📁" },
    { url = "browser-qutebrowser/", text = "🌐" },
    { url = "config-faelight/", text = "⚙️" },
    
    # ──────────────────────────────────────────────
    # 📚 IMPORTANT DOCS
    # ──────────────────────────────────────────────
    { url = "README.md", text = "📘" },
    { url = "CHANGELOG*.md", text = "📋" },
    { url = "THEORY_OF_OPERATION.md", text = "🧠" },
    { url = "INTENT/", text = "🎯" },
    
    # ──────────────────────────────────────────────
    # 🔧 SCRIPTS & TOOLS
    # ──────────────────────────────────────────────
    { url = "scripts/", text = "🔧" },
    { url = "rust-tools/", text = "🦀" },
    { url = "*.sh", text = "📜" },
    
    # ──────────────────────────────────────────────
    # ⚙️  CONFIG FILES
    # ──────────────────────────────────────────────
    { url = "*.toml", text = "⚙️" },
    { url = "*.yaml", text = "⚙️" },
    { url = "*.yml", text = "⚙️" },
    { url = "Cargo.toml", text = "📦" },
    
    # ──────────────────────────────────────────────
    # 🔗 SYMLINKS
    # ──────────────────────────────────────────────
    { url = "*", is = "link", text = "🔗" },
    
    # ──────────────────────────────────────────────
    # 📁 DIRECTORIES
    # ──────────────────────────────────────────────
    { url = "*/", text = "📁" }
]
