[package]
name = "keyscan"
version = "3.0.0"
edition = "2021"
description = "Sway keybinding analyzer - find conflicts and duplicates"

[dependencies]
# Core infrastructure
faelight-core = { path = "../faelight-core" }

# Modern CLI
clap = { version = "4", features = ["derive"] }

# Beautiful output
colored = "2"

