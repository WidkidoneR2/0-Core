//! faelight-term v2 -- GPU Renderer (Phase 0 stub)
//! wgpu pipeline: glyph atlas, cell grid, cursor.
//! Phase 0: stub -- will be implemented gate by gate.
pub mod atlas;
pub mod grid;
pub mod cursor;
/// Phase 0 stub -- full implementation in Phase 0 gates
pub struct Renderer;
impl Renderer {
    pub fn new() -> Self {
        Self
    }
}
