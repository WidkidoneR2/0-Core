# DEPRECATED

This tool has been superseded by `core` v2.

| Old command | New command |
|---|---|
| security-audit scan | core security scan |
| security-audit report | core security report |

The `scripts/security-audit` wrapper redirects all calls to `core` automatically.
Source retained for reference until Phase 5 cleanup.
