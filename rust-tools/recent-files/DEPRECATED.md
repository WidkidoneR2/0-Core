# DEPRECATED

This tool has been superseded by `core` v2.

| Old command | New command |
|---|---|
| recent-files [range] | core workspace recent [range] |

The `scripts/recent-files` wrapper redirects all calls to `core` automatically.
Source retained for reference until Phase 5 cleanup.
