[package]
name = "faelight-stow"
version = "2.0.0"
edition = "2021"
description = "Stow package manager - verify and manage symlink packages"

[dependencies]
# Core infrastructure
faelight-core = { path = "../faelight-core" }

# Modern CLI
clap = { version = "4", features = ["derive"] }

# Beautiful output
colored = "2.1"
