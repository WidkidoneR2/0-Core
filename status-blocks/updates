#!/bin/bash
# Get update count from faelight-update
COUNT=$(faelight-update --count-only 2>/dev/null | tail -1)

# Write to cache for bar
if [[ -n "$COUNT" ]]; then
    echo "$COUNT" > "$HOME/.cache/faelight-bar/updates"
else
    echo "0" > "$HOME/.cache/faelight-bar/updates"
fi
