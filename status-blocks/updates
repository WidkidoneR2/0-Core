#!/bin/bash
CACHE_FILE="$HOME/.cache/faelight/update-count"
if [[ ! -f "$CACHE_FILE" ]]; then
    echo ""
    exit 0
fi
UPDATES=$(cat "$CACHE_FILE")
# Only show if there are actual updates, hide the checkmark
if [[ "$UPDATES" == " ✓" ]]; then
    echo ""
else
    echo "$UPDATES"
fi
