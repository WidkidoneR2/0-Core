[package]
name = "zero-docs"
license = "MIT"
version = "2.0.0"
edition = "2021"
description = "Living documentation engine — keeps README and welcome in sync with live state"

[dependencies]
zero-core = { path = "../zero-core" }
colored       = "2.1"
dirs          = "5.0"
chrono        = "0.4"
rusqlite      = { version = "0.31", features = ["bundled"] }
